import { StyleSheet } from 'react-native';

const styles = {
  container: {
    height: '60%',
    flexWrap: 'wrap',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    alignContent: 'center',
  },
  tile: {
    backgroundColor: 'black',
    borderWidth: 1,
    borderColor: 'grey'
  }
}

export default styles;